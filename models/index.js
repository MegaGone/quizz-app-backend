const Server    = require('./server')
const User      = require('./user');
const Quiz      = require('./quiz');
const Role      = require('./role')

module.exports = {
    Server,
    User,
    Quiz,
    Role
}